# Legacy Dolt Archive Runbook

The current operator recovery runbook is
[`docs/runbooks/beadstore-recovery.md`](../../../runbooks/beadstore-recovery.md).

Oro now uses the native beadstore; the former Dolt-specific archive runbook is
no longer retained.

This compatibility pointer is retained because the original `oro-77rx`
acceptance command checked this path.
