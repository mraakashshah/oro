---
date: 2026-04-01
status: partial
---

goal: >
  Explored Claude Code source (nirholas/claude-code, cloned to archive/yap/reference/claude-code).
  Identified patterns worth bringing to oro. Brainstormed interactively, narrowed from 5 enterprise
  patterns to 2 focused specs: memory dreaming + web dashboard. Memory dreaming epic completed
  by swarm. Dashboard beads need recreating.

now: >
  1. Recreate dashboard beads (lost in bd init --force). Use the bead definitions from the
     conversation — they were fully spec'd with Rule of Five. Epic ID was oro-2hb but that's
     gone too. Design doc is at docs/plans/2026-03-31-web-dashboard-design.md (R2 PASS).
  2. Focus swarm on dashboard epic once beads exist.
  3. The enterprise readiness design doc (docs/plans/2026-03-31-enterprise-readiness-design.md)
     is archived reference — user cut 4 of 5 patterns during brainstorming. Don't execute it.

test: |
  go test ./pkg/memory/... ./pkg/ops/... ./pkg/dispatcher/... ./pkg/worker/... -run 'Dream|Staleness' -count=1 -timeout 180s
  # Memory dreaming should pass (already merged)
  # Dashboard has no code yet

done_this_session:
  - task: "Cloned nirholas/claude-code to archive/yap/reference/claude-code"
    files: [archive/yap/reference/claude-code/]

  - task: "Wrote enterprise readiness design doc (5 patterns), adversarial reviewed R1+R2"
    files: [docs/plans/2026-03-31-enterprise-readiness-design.md]
    notes: "User cut 4 of 5 patterns. Only memory dreaming + dashboard survived brainstorming."

  - task: "Wrote memory dreaming & staleness design doc, adversarial reviewed R1+R2"
    files: [docs/plans/2026-03-31-memory-dreaming-design.md]

  - task: "Wrote web dashboard design doc, adversarial reviewed R1+R2"
    files: [docs/plans/2026-03-31-web-dashboard-design.md]

  - task: "Fixed 7 pre-existing CI failures"
    files: [pkg/worker/prompt_test.go, cmd/oro/cmd_init_test.go, cmd/oro/cmd_work_test.go, cmd/oro/dolt_test.go, cmd/oro/cmd_dolt_test.go, cmd/oro/cmd_init.go]
    notes: >
      stealth mode migration broke init/work tests (missing --local flag, empty defaultBranch),
      prompt text changed without test update, dolt tests fail when server already running,
      wrapcheck lint on cmd_init.go

  - task: "Memory Dreaming epic completed by swarm (oro-kpwx)"
    files: [pkg/ops/ops.go, pkg/ops/dream_prompt.go, pkg/memory/dream.go, pkg/memory/memory.go, pkg/worker/prompt.go, pkg/dispatcher/dispatcher.go]
    notes: >
      5 beads merged: OpsDream type, dream action parser, DumpAll/MergeMemories,
      ForPrompt staleness age column, dispatcher wiring (trigger every 10 beads + epic close).
      Epic merge commit: aeaa362

blockers:
  - "Dashboard beads lost in bd init --force — need recreation"
  - "ff-only merge failures are a recurring pattern — 4+ beads hit this during session. Workaround: cherry-pick or manual merge. Root cause: worktrees branch from old main, rebase + ff-only fails when main has moved significantly."

decisions:
  - cut_enterprise_patterns: >
      User cut observability (Prometheus), audit, containers, MCP, IDE bridge.
      Reason: CTO pitch is about vision not features. Focus on making agents
      smarter (memory) and making the swarm visible (dashboard).
  - dashboard_replaces_oro_mg: >
      Web dashboard on localhost:4444 replaces bubbletea TUI (45K LOC, slow).
      htmx + SSE, Go templates, no JS build step. Linear layout + Mardi Gras soul.
  - parade_not_kanban: >
      Keep parade metaphor (beads flow through pipeline). Kanban implies human
      drag-and-drop; parade implies watching a procession. Sections: Queued Up,
      Rolling, Stalled, Finished.
  - dreaming_trigger: "Every 10 completed beads OR on epic close. Full create/merge/delete power."
  - staleness_threshold: "7 days hardcoded in ForPrompt, not a Config knob."

worked:
  - "Interactive brainstorming with one question at a time — user shaped scope dramatically"
  - "Adversarial review caught real wiring gaps (wrong call sites, missing result callbacks)"
  - "Swarm completed memory epic autonomously (5 beads, 4 merges, 1 conflict auto-resolved)"

failed:
  - "Jumped to beadcraft before brainstorming — user caught it, had to delete and restart"
  - "bd init --force wiped the database — lost all beads, had to recreate manually"
  - "ff-only merge failures blocked multiple beads — dispatcher rebase/merge pipeline fragile"
  - "Workers kept implementing spawnDream without wiring DumpAll — needed 3 rejections + AC rewrite"

next:
  - "Recreate dashboard beads (7 tasks) — see conversation for full bead definitions"
  - "Focus swarm on dashboard epic"
  - "Monitor for ff-only merge failures — may need to investigate dispatcher merge pipeline"

files:
  created:
    - docs/plans/2026-03-31-enterprise-readiness-design.md
    - docs/plans/2026-03-31-memory-dreaming-design.md
    - docs/plans/2026-03-31-web-dashboard-design.md
    - archive/yap/reference/claude-code/
  modified:
    - pkg/worker/prompt_test.go
    - cmd/oro/cmd_init_test.go
    - cmd/oro/cmd_work_test.go
    - cmd/oro/dolt_test.go
    - cmd/oro/cmd_dolt_test.go
    - cmd/oro/cmd_init.go
    - pkg/ops/ops.go
    - pkg/ops/dream_prompt.go
    - pkg/memory/dream.go
    - pkg/memory/memory.go
    - pkg/worker/prompt.go
    - pkg/dispatcher/dispatcher.go

beads:
  completed: [oro-f7zv, oro-ysco, oro-lle.1, oro-f6bv.2, oro-c8qj, oro-kpwx]
  in_progress: []
  remaining: ["dashboard beads need recreation — 7 tasks from web dashboard design doc"]
  epic: "oro-kpwx (memory) DONE. Dashboard epic needs recreation."

dashboard_bead_definitions: >
  These 7 beads were fully spec'd but lost in bd init --force. Recreate from conversation:
  W1: Add Blocked() and Closed() to BeadSource interface (5m)
  W2: Implement SSEBroadcaster with Subscribe/Unsubscribe/Send (5m)
  W3: Hook SSEBroadcaster.Send into logEvent and logEventLocked (5m, dep: W2)
  W4: Define DashboardData interface and implement on Dispatcher (7m, dep: W1+W2)
  W5: Add HTTP server to dispatcher Run() with --web flag (7m, dep: W4)
  W6: Create HTML templates, CSS, and go:embed for web dashboard (7m)
  W7: Implement HTTP handlers for full page, fragments, and SSE endpoint (7m, dep: W4+W5+W6)
