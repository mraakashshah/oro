prompt-template-interpolation: template uses `<placeholder>` for agent-filled values → prefer `fmt.Sprintf` with real values when the value is known at prompt-assembly time
`hint-duplication`: two places define the same key bindings (help overlay and status bar hints) → consider a single source of truth in a future task.
`tmux-pre-attach-setup`: need to configure tmux state before attach → use `select-window`/`select-pane` via Runner before the `exec.Command` attach call.
case-insensitive-header-search: matching markdown headers → lowercase both sides, match on lowered text, slice from original to preserve casing.
`scope-creep`: task does documentation cleanup but also removes 3 unrelated Go features → split into separate tasks for traceability
`hook-install-worktree`: when symlinking git hooks via `$(pwd)`, verify behavior in worktree contexts where `.git` is a file, not a directory — worktrees share hooks from `$GIT_COMMON_DIR/hooks/`, so `make install-git-hooks` must be run from the main repo root to be effective for all worktrees.
`dead-code-noop`: function/method calls replaced with blank identifier assignments (`_, _ = fn, arg` or `_ = variable`) → CRITICAL. This silently destroys functionality while still compiling. Common during QG retry when worker misinterprets lint errors. The correct fix for unused variables is to remove the declaration, NOT to assign the function call to `_`.
`idempotent-stat-guard`: filesystem cleanup needs retry safety → `os.Stat` pre-check returning nil on `os.IsNotExist` before the actual operation
`stale-nolint-comment`: changing the derivation of a value → update the nolint justification comment to match the new source
`stealth-detection-duplication`: readProjectName and detectProjectMode share stealth hash logic → extract shared helper when a future task touches either function.
`path-mode-duality-test`: function handles both relative defaults and absolute stealth paths → test both modes plus the conversion boundary (in-repo absolute → CWD-relative).
`fallback-on-empty-field`: when replacing a hardcoded path with a configurable field, check `if field == ""` and fall back to the legacy default — ensures zero-value Config still works
`lenient-vs-strict-resolution`: two functions resolve the same identity with different "not found" semantics → extract shared core, wrap with policy.
`zero-value-fallback`: new struct field with configurable path → check `if field == "" { use default }` at point of use, keeping zero-value safe for existing callers.
`find-exclusion-abs-passthrough`: absolute path outside repo tree used in `find . -not -path` exclusion → exclusion is unreachable. Consider omitting it from the generated script rather than including a dead clause.
`extracted-default-helper`: pure function with multiple interrelated defaults risks cyclomatic bloat in `withDefaults()` → extract a named helper (e.g., `defaultWorkerCounts`) that resolves defaults + cross-field clamping in isolation.
`double-hash-computation`: function computes identity hash, caller also computes same hash for display → return computed value from inner function to avoid redundant I/O.
`best-effort-symlink`: new worktree asset needs linking → add to `stageAssets` with `slog.Warn` on failure, no error return, Lstat guard for idempotency.
`stale-hardcoded-after-wiring`: replacing a hardcoded value in behavior but leaving it in nearby comments/log strings → search the same function and callers for other instances of the old hardcoded value.
`error-path-db-cleanup`: DB record created before fallible operation → add cleanup call in error path, log but don't block subsequent cleanup steps.
`double-nowFunc-under-lock`: calling `d.nowFunc()` multiple times under a single lock hold → capture once in `now := d.nowFunc()` and reuse.
`coalesce-scan-consistency`: new query scans nullable-with-default column differently from all existing queries in the same file → match the established COALESCE + int scan pattern for consistency.
`vestigial-encoder-field`: all send paths now use sendToWorker → encoder field on trackedWorker is dead code, candidate for removal in a cleanup task.
`ac-vs-reality-check`: task AC claims function is unused → grep production callers before deleting
`buffer-before-response`: HTTP handler executes template into `bytes.Buffer` first → prevents partial HTML on error, enables atomic content-type + body write
`buffer-template-render`: template renders to `http.ResponseWriter` incrementally → buffer into `bytes.Buffer`, check error, then `buf.WriteTo(w)` for atomic all-or-nothing HTTP responses.
`like-escape-clause-reminder`: pure escapeLike function escapes with backslash → caller must add `ESCAPE '\'` to the SQL LIKE expression for SQLite to honor the escaping.
test-hook-on-prod-struct: adding a `testXxxFn func()` field on a production struct that is read without holding the struct's mutex → either guard the read with the same lock or gate the field behind `_test.go`-only extension.
worker-internal-monologue: test comments read like chat-thinking ("wait", "let me reconsider", "actually") → strip before landing; comments describe the scenario, not the author's deliberation.
backoff-fn-injection: new retry/recovery path needs deterministic tests → add `somethingBackoffFn func(int) time.Duration` field on Dispatcher, gate real backoff through `d.somethingBackoff(n)`, let tests set it to `return 0`. Mirror the `loopPanicBackoffFn` pattern (dispatcher.go:619-633).
misnamed-negative-test: test name claims to verify a failure path but body only smokes the success path → rename to match what's asserted, or delete if redundant with the primary contract test.
helper-migration-leaves-redundant-setup: replacing `sql.Open` with a helper that encapsulates PRAGMA setup → search the call site for manual `PRAGMA journal_mode` / `busy_timeout` execs that are now no-ops and remove them in the same edit.
redundant-pragma-after-openDB: test opens via `dbutil.OpenDB` but keeps the old explicit `PRAGMA journal_mode=WAL`/`busy_timeout` calls from its pre-migration state → remove the redundant Execs, rely on OpenDB's contract.
misleading-nolint-justification: adding `//nolint:foo // reason` whose reason doesn't match the file's actual code → verify the justification is true at the time of writing, or drop it.
wrapcheck-vs-thin-delegation: AC asks for `return other.Fn()` single-line delegation but `wrapcheck` is enabled → either add `//nolint:wrapcheck` with justification or accept a redundant outer wrap; document choice in task.
dbutil-openDB-probe-tautology: WAL-verification test using dbutil.OpenDB as the probe → probe may set WAL itself, masking regressions in the unit under test; use a raw sql.Open probe or file-header inspection.
ac-contradiction-resolution: AC signature and edges conflict → satisfy the concrete edge case in tests, note the deviation.
optional-capability-interface: core interface shouldn't force capabilities all impls support → extract separate interface (e.g., VocabPersister) and type-assert at the call site.
interface-unused-until-followup-task: introducing interface + renaming concrete in one task, widening callers in follow-ups → keep concrete receivers in call sites this task to preserve shape compatibility; land widening after dependents rebase.
interface-widen-struct-rename: widening Store field to interface named same as existing concrete struct → rename struct (e.g. Embedder→TFIDFEmbedder), define interface + optional extension, add compile-time assertions in test file
parenthetical-token-noise: comma-split field has inline `(annotations)` with dots or numbers → strip `\s*\([^)]*\)` from each token before path heuristics
cli-doc-existing-directive: documentation task exposing already-implemented protocol behavior → CLI test with stateful mock dispatcher; semantic correctness lives in the dispatcher package's own tests.
stale-ac-field-name: AC references field name that conflicts with linter → update doc comments to match the actually-chosen identifier, not the AC's original spec
placeholder-digest-registry: static registry ships with placeholder hashes → mark with `TODO(task-id)` so future task that fills in real values is easy to locate.
yaml-tri-state-bool: YAML field must distinguish unset/true/false → `*bool` with `XOrDefault()` accessor returning the default when nil (keeps zero-value round-trip safe).
installer-test-hook: local-tarball test needs to bypass curl+checksum → add `_ORO_TARBALL_OVERRIDE` env var branch + PATH-prepended mock binary writing to a log file.
ac-test-vs-assert-mismatch: AC "Test" column enumerates N tests but "Assert" column describes N+1 behaviors → either add the missing test or prune the assertion from AC before closing.
vendored-arch-specific-static-archive: checking in a `.a` under a `cgo && darwin` tag when the archive is arm64-only → either narrow tag to `arm64` or fetch/bundle per-arch in installer before the epic lands.
cancel-vs-result-race: buffered resultCh + select on ctx.Done → document that the resource from a late-arriving result may need disposal if it owns external handles.
rate-limiter-default-injection: production worker needs configurable rate limiter for testing → declare struct field as `interface{ Wait(context.Context) error }`, fall back to `rate.NewLimiter(rate.Limit(N), 1)` when nil, tests inject `rate.NewLimiter(rate.Inf, 1)` for instant runs
best-effort-retention-prelude: synchronous trim before periodic background work → log via existing channel, never return error, no new goroutine
ad-hoc-cli-split: extraction library + thin `cmd/main.go` flag wrapper → keeps pure logic testable while satisfying "extract.go or equivalent" CLI requirement.
swallowed-handler-error: conn-handler dispatches to sub-handler returning error → assign to `_` → log-and-drop becomes silent-drop, lose observability on write failures.
stage-assets-prereq: running `go test ./...` in a fresh worktree hits embed.go `_assets` pattern miss → run `make stage-assets` before any full-suite verification.
format-string-vs-named-constant: `%.2f` literal in error/log message duplicates a nearby `const` → reference the constant via the format arg so the two stay in sync.
stale-hardcoded-after-wiring: moving a function out of a file → grep for identifiers only that function used, delete the now-orphan private constants in the old file.
unused-asset-shipped: AC requires committing a data file alongside code that embeds the same data inline → file and inline source will drift; note in task that a successor should unify (load from file or delete the duplicate).
ac-tag-vs-convention: AC specifies build tags that conflict with package-wide integration test convention → follow package convention and document the deviation in the output artifact.
deferred-setupConfig-wiring: task scopes down the design's Embedder+VectorIndex+Reranker setupConfig contract to just Embedder → file a follow-up task and note the partial contract in a `TODO(task-id)` comment on `setupConfig`.
qg-absorb-pre-existing-fixes: QG on your branch flags issues pre-existing on main → absorb small fixes in separate commits with descriptive messages + memory note, rather than blocking the task or reverting.
assign-loop-wake-on-removal: any code path that removes a managed worker from `d.workers` → call `d.notifyAssignLoop()` before returning, so `reconcileScale` rebalances within one tick rather than waiting for the fallback ticker.
redundant-timeout-branch-after-union: widening an earlier `continue`-guarded condition to absorb a later branch's trigger state → later branch becomes unreachable in default config; either note the config dependency in a comment or test both config regimes.
injectable-derive-fn: pure deterministic helper needs test control → expose as package-level `var foo = realFoo` with `//nolint:gochecknoglobals` and let tests save/restore around the call
`omitempty-test-bait`: subtest named "omitempty tag does X" but body never marshals a zero-value struct → either marshal `T{}` and assert key absent, or rename to describe the actual setter-side behavior verified.
daemon-only-bypass: cobra RunE forks between startFreshSwarm and runDaemonOnly → safety checks added to one path must be mirrored (or hoisted) to the other, or explicitly scoped in AC.
word-boundary-grep-false-positive: AC uses `\barchitect\b` → underscore-prefixed identifiers like `architect_router.py` don't match (underscore is a word character), so test-fixture filenames that filter oro-specific hooks can remain legitimately.
base-lag-readme-diff: branch based on pre-doc-cleanup commit shows phantom doc additions in `git diff main` → verify the commit's own file list before flagging README scope drift; 3-way merge resolves naturally.
window-name-over-index: tmux pane refs in user-facing docs → prefer named window (`oro:manager`) over numeric index so docs survive single-window vs multi-window modes.
stale-stub-after-rename: renamed method/parameters leave fake-runner stub keys matching the OLD call signature → audit `fake.errs` / `fake.output` keys post-rename; tests can pass-by-coincidence when the stub becomes a no-op.
orphaned-test-after-source-delete: deleting a hook/beacon source → grep `tests/*.py` for `import <module_basename>` and delete those orphan tests in the same bead so QG stays green.
stay-deleted-file-guard: regression guard for a deleted-file invariant → assert via `os.Stat` from within the package's `_test.go`, embed the deletion's task ID and the bug's stderr signature in the failure message.
phantom-delete-from-base-lag: `git diff main` shows file deleted, but commit only adds → check `git log base..main -- <file>`; if main added it after branch base, deletion is phantom and 3-way merge preserves it.
escalation-trigger-retry-mirror: adding a precheck for an existing escalation → copy the trigger's marker-detection logic verbatim, add status/type short-circuits for resolution paths.
cli-doc-existing-directive: hook test asserts substring content of generated tmux hook → semantic correctness of dispatcher-side receipt belongs in a dispatcher-package integration test that wires `nc -U` against the listener.
anchor-splice-forward-progress: matching ordered-anchor sequences against a host array → forward-only search from `lastMatch+1` enforces order rule for free, and naturally rejects unreachable duplicate-text cases.
idempotent-alter-via-error-string-match: SQLite ALTER TABLE ADD COLUMN needs idempotency → exec then catch `strings.Contains(err.Error(), "duplicate column name")` as no-op; pair with `CREATE TABLE/INDEX IF NOT EXISTS` for the rest.
pure-byte-extractor-split: tree-sitter extractor takes a file path at the boundary but delegates to a `extractGoSymbols(src []byte)` inner that's purely byte-driven → clean for unit testing without touching the FS.
phantom-deletion-stale-base: `git diff main` shows deletions for files added on main after branch was cut → confirm against merge-base, not main HEAD; 3-way merge resolves naturally.
view-text-match-vs-behavioral: testing migration view changes by string-matching `sqlite_schema.sql` for keywords → write a behavioral test that seeds rows exercising the new clause and asserts membership in the view, mirroring the CHECK-constraint test pattern the same file already uses.
shift-vs-scale-comment: function normalizes via additive offset → comment should say "shifted" not "scaled"; reserve "scaled" for multiplicative behavior.
value-space-comment-drift: enum-string field gains a new value when a new caller is added → struct field's docstring listing valid values must be updated, otherwise it lies to readers.
stale-section-numeric-comment: inserting a new conditional section between numbered comments → renumber subsequent siblings (e.g., bump `// 3c.` → `// 3d.`) so comments stay monotonic.
abspath-leak-into-prompt: building filesystem paths via `filepath.Join(worktree, rel)` for prompt content → pass the original relative path so prompts are reproducible across worktrees/machines.
unwrap-preserves-errors-Is: when converting an error sentinel to a richer struct error → add `Unwrap() error` returning the original sentinel so existing `errors.Is(err, ErrSentinel)` call sites keep working unchanged.
dispatch-untested-when-parsers-tested: when test coverage is per-implementation but a switch-statement dispatcher is the production API, add a one-line dispatch test or the dispatch can silently regress.
openStateDB-backfill-idempotency: backfill wired into every-open path → use NOT EXISTS gate per-event-type so subsequent opens are no-ops without per-task bookkeeping.
contract-test-method-count-without-signatures: bumping `len(methods) != N` count when adding interface methods → also add `assertSignature` calls for each new method, otherwise signature drift on the new methods is silent.
misleading-justification-comment: comment explains why an error is ignored ("X returns an error") but the code under test never produces that error → either delete the justification or assert no error.
`walker-caller-context: enclosing-symbol tracker` → save/restore `prev` around recursive descent so nested decls don't leak caller scope (call_graph.go:93–97 reference impl).
bench-gate-platform-anchored: spec anchors latency targets to specific hardware (e.g. M-series Mac) → gate bench test with matching `//go:build darwin && arm64 && !race` and document the reason in the file header so CI on incompatible runners skips cleanly instead of producing flaky failures.
dirname-as-pkg-key: cross-package resolution that keys on `filepath.Base(filepath.Dir(fp))` → document that declared `package X` mismatches with dir name will silently mis-key; defer to indexer.
tx-bound-cross-store-snapshot: cross-store reads need consistent view → expose `Cards()`/equivalent on the parent's ReadTx that wraps the same `*sql.Tx`, and prove with an external-conn write test, not just a self-consistency test.
worker-msg-colon-namespace: prompt copy uses `oro edit:replace` colon form to namespace tool families → matches existing oro-kvep convention; if Phase C.3 implementations require shell invocation, add argv preprocessing or alias routing rather than rewriting all worker copy.
bench-gate-darwin-arm64: latency budget gate sensitive to race detector + arch → `//go:build darwin && arm64 && !race` with `benchWarmup=10`/`benchSamples=200`, helper that asserts p50/p99 against spec table cells.
dual-parented-cobra-command: same operation needs both a subcommand under a group AND a top-level alias → use factory functions returning fresh `*cobra.Command` instances, never share between parents (cobra mutates `Parent` on `AddCommand`).
phased-prompt-stub: advertising tool surface in worker prompt before backing implementation lands → workers waste iterations on `not yet implemented`. Add scaffold caveat in the prompt section, or gate the section until impl lands.
stale-rule-branch-coverage: validation function with N-branch reject path → tests should exercise each branch separately, not just the headline case.
monologue-prefix-in-corpus-traces: line-by-line classification traces in corpus tests get prefixed with "Wait:"/"Actually:" → strip the deliberation prefix; keep only the declarative trace.
detach-before-kill: respawn flows that terminate a subprocess via procMgr.Kill must clear `worker.beadID/assignmentID/epicID` (state→ShuttingDown) *before* the kill — otherwise the deferred `connCloseCleanup` observes the still-attached bead and wipes pending handoff/tracking state. Mirror `shutdownWorkerForHandoff`.
corpus-runtime-guard: bare `len(cases)==N` check passes silently when group sizes drift → use per-group/per-language/per-error-class count maps that fail loudly on rebalance.
spec-deviation-with-rationale: signature trimmed from spec sketch (e.g. dropping unused store/cards params) → land with the trimmed signature and document the deviation in the doc comment naming the future phase that re-introduces the dropped deps. router.go:18-23 is a clean example.
`diagnostic-cli-mirror-of-resolver`: a diagnostic CLI that mirrors a runtime resolver's priority/parsing logic → have the CLI call the runtime resolver, not reimplement it, to prevent drift.
`loaded-but-unwired-config-field`: new Config field + DB column added in a "lay-down" task with no production consumer → annotate the resolver with a `TODO(<follow-up bead>)` so future readers know the field is intentionally dormant until the wiring task lands.
gate-before-callers: lint+state-machine landed alone as a sequencing task → downstream beads must route through `transitionPipeline`; lint enforces the contract before any caller exists.
no-assertion-ticker-test: ticker subtest configures fast interval and exits on ctx.Done with no behavioral assertion → either assert observable side effect (DB row state change) or fold into the cancel-stop test.
linter-syntactic-receiver-name: lint rule keys off receiver-name selector → narrow but tractable; document as "guardrail for the established pattern" rather than "general check."
single-readtx-via-counter-decorator: render commands need to assert "exactly one read-tx span covers the snapshot" → wrap the `Store` with a thin counter decorator that delegates `WithReadTx` and increments `count`, then assert `counter.count == 1`. Real test hook, not a tautology.
closecheck-substring-buildtag: substring match on `go:build <tag>` over all comments → false-exempt files where the phrase appears in regular comments; restrict to leading constraint comments.
completion-check-wiring-gap: new dispatcher method shipped with full unit-test coverage but zero production callers → file a follow-up wiring bead so the §-contract doesn't go dead.
tri-state-flag-via-Changed: distinguishing "user passed default value" from "flag unset" → `cmd.Flags().Changed("name")` instead of value comparison.
phantom-empty-merge-task: branch SHA == main SHA, AC already satisfied → verify via grep + per-line read, treat as already-landed; flag pre-existing test failures as separate work, not regressions of this diff.
ours-merge-drops-content: a literal `git merge -s ours` records a parent without taking that parent's tree → never use it to advance an epic over main; use a content merge and verify a tree-neutral merge has not dropped files newly added by a later parent.
test-fake-scoring-divergence: in-memory test fake of an interface that does ranking → document any scoring divergence (missing relevance multipliers, threshold-only filtering) in a comment on the fake type so future render-path tests don't silently rely on the simplified semantics.
cgo-gate-shared-types: gating tree-sitter callers behind `//go:build cgo` → split shared types (e.g. `CallEdge`, `Symbol`, `ImpactResult`, `ErrXRequired` sentinel) into untagged files so non-CGO dependents still compile against stub functions.
fake-tx-snapshot: in-memory fake of a transactional read interface → take a `make`+`copy` snapshot under RLock at tx entry, hand it to the txn struct, release lock before invoking fn — gives stable read view + permits concurrent mutation by tests.
cgo-gated-package-doc: package doc on a build-tagged file disappears under the opposite tag → place package-level doc on a file with no build constraints.
wall-clock-p99-budget-vs-sample-count: when increasing N for percentile stability, recompute the statistical position before also relaxing thresholds — combining both can erode spec budgets unnecessarily.
qg-coverage-boundary-flake: package coverage hovering at QG threshold (e.g. 85.0%/84.9%) → add tests for genuinely-uncovered public surface (method coverage gaps detected via `go tool cover`) rather than tweak the gate.
fake-vs-prod-semantic-divergence: in-memory test fake replicates production interface but diverges on ordering/scoring/loop semantics → document divergences in godoc OR mirror production helpers (e.g., reuse `buildInlined`/`toSummary` if they were exported) so test authors don't unknowingly write tests that pass against the fake but would fail against production.
read-tx-coverage-completion: new ReadTx interface gets one Show smoke test → follow up with List filter/limit/offset and Relevant ordering tests in the same TestCardStoreRead block before declaring the interface covered.
dead-subpackage-deletion-needs-testonly: deleting a subpackage whose siblings only had it as production caller → annotate now-test-only exports in siblings with `//oro:testonly` in the same commit, explain in commit message, and add a `forbidden_imports_*_test.go` invariant guard to prevent re-introduction.
additive-projectpaths-field: new path component plumbed through ProjectPaths → mirror entries in both `standardProjectPaths` and `stealthProjectPaths`, add a single dual-mode test that asserts both branches.
repo-tracked-config-dir-ignore: when a repo tracks files inside a directory that is otherwise oro-managed (e.g. `.oro/config.yaml` lives in oro itself), repo-level `.gitignore` needs anchored per-file entries for any locally-generated siblings, while `oroGitignoreEntries()` keeps the bulk `.oro/` ignore for target projects.
additive-check-constraint-update: widening a CHECK constraint inside an idempotent `ALTER TABLE ADD COLUMN` → silenced on existing DBs; needs a separate migration step (recreate-and-rename) when constraint must apply to old data.
read-only-inbox-viewer-first: introducing a new file path the system will eventually produce → ship the read-only viewer + ProjectPaths field first, defer producer to a separate task; keeps blast radius small and gives users immediate visibility.
standard-mode-equals-fallback-path: test verifies wired field with a value that coincidentally matches the fallback → use stealth/custom path so the expected value differs from the fallback default.
package-collapse-on-sole-consumer: a subpackage with exactly one importer is a candidate for collapse → relocate files into the consumer package and add a forbidden-imports test to lock the deletion.
paths-field-vs-hardcoded: production code writes a path also represented by a `ProjectPaths` field → wire the resolved field through Config instead of hardcoding the same string, so stealth-mode resolution stays consistent.
humanized-duration-formatter: relative-time labels needed in CLI output → switch on `time.Minute / time.Hour / 24*time.Hour` with integer truncation, sentinel `"-"` for unparseable, abs() the diff to absorb clock skew.
forbidden-imports-test-after-full-deletion: AC's `_test.go` exclusion list in a forbidden-imports check becomes unreachable once the imported package is fully deleted (any reference fails to compile, never reaches the test) → either remove the exclusion or document it as guard against accidental package reintroduction.
forward-compat-unused-param: AC prescribes a signature with a parameter not yet consumed → name with `_` so intent (future use, not dead code) is visible at the signature.
deprecated-field-passthrough: struct field kept post-behavior-removal with deprecation comment → callers continue passing values that are silently dropped; document the regression and link the cleanup follow-up task.
ac-edges-vs-task-title-conflict: AC edges forbid touching sibling subpackages but task title says "move into X package" → satisfy the task title, document deviation; named-excluded packages (data/views) still must not be touched.
bead-help-translation: write Long/Short/Use using bead terminology so `adaptTaskCommandHelp`'s string replacer surfaces task-primary copy on the task subtree while bead help remains the legacy alias.
ac-grep-substring-false-positive: AC uses `grep -c 'test_architect'` substring → matches unrelated test method names like `test_architect_beacon_loaded` → AC should anchor with prefix or path qualifier (e.g. `grep '^tests/test_architect_router'`).
rg-shell-out-test-silent-pass: test shells out to `rg` and discards exit code → false-negative if rg missing or errors → assert `exitErr.ExitCode() == 1` before treating empty output as success.
docstring-spec-contradiction: test docstring "Then" disagrees with assertions while title/comments still match the assertions → revise the Given/When/Then field to match what the body actually verifies.
orphan-allowlist-after-source-delete: deleting hook test files → grep `tests/test_asset_mirrors.py` `allowed_extra_paths` and any terminology allowlist regex for the deleted basenames; drop the stale entries in the same bead.
dual-write-shim: temporary mirror writer during migration window → wrap primary store, synchronous mirror, log-and-continue on mirror failure, ship a CheckDrift + CLI for operational verification.
subprocess-script-test: integration test that wraps a bash smoke script → keep the Go side a thin subprocess invocation with `cmd.CombinedOutput()` + `t.Fatalf` on non-zero exit; the script owns the assertions, the Go test owns the build-tag/short-mode integration boundary.
deletion-as-rename: "delete file X" with live callers → rename to descriptive name + delete companion `_test.go` + add `os.Stat` regression test, document scope in test docstring
base-lag-phantom-diff: branch shows changes to unrelated files in `git diff main` → verify the branch's own commits (`git show --stat <commits>`) before flagging scope drift; merge resolves naturally.
per-entity-identity-not-global-head: when a code path captures the "result identity" of one entity in a stream of concurrent operations on a shared HEAD → read the entity's own ref (e.g. branch tip) rather than HEAD, which the next operation will advance.
vestigial-call-site-after-swap: replacing only the production caller of a writer leaves the writer + its tests as dead production code → file follow-up cleanup bead in same PR description.
wiring-without-consumer: Config field added and populated but no consumer reads it yet → file `TODO(<task-id>)` on the future consumption site or land a follow-up bead so the field doesn't decay into dead config.
tombstone-test-on-delete: deleting a file/symbol → add `forbidden_imports_<name>_test.go` that asserts `os.Stat` returns ErrNotExist for the file AND greps for the deleted public symbols, so future re-introductions fail red.
empty-package-guard-test: deprecating a package by removing its source while leaving a `package foo_test` file in the directory → `go test ./pkg/foo/` still compiles and runs (Go allows test-only directories), giving you a regression guard without a vestigial source file.
nonblocking-side-effect-helper: helper is called for its side effect inside an approval/critical path → caller must log+continue (no early return), the helper itself returns error without logging to avoid double-log, and the helper must zero-value-fallback (empty path → return nil) so unwired callers stay safe.
process-liveness-via-signal-zero: managed worker may keep heartbeating after OS process exits → probe with `proc.Signal(syscall.Signal(0))`, mock with explicit dead-id set.
dual-verdict-spec-vs-quality: a change is not done after one blended pass → judge it on TWO distinct verdicts before approving: (1) SPEC COMPLIANCE — does it do exactly what the AC/task asked, nothing more (scope creep) and nothing less (missing requirement); (2) CODE QUALITY — is what it does well-built. A diff can be clean code that solves the wrong problem, or the right behavior built badly; both fail. Reach each verdict separately, then roll them into the terminal verdict.
reviewer-anti-pre-judging: never let a review be primed to suppress findings → a reviewer handed pre-rated severities ("treat as Minor at most"), a "do not flag X" instruction, or a scope narrowed so a defect can't surface will rubber-stamp it. Reach your own verdict from the diff; if a narrowed scope is given, still surface any direct critical regression you hit while validating it. The `dead-code-noop` (`_, _ = fn, arg`) gate-passing trick is exactly what an un-primed reviewer catches and a primed one waves through.
