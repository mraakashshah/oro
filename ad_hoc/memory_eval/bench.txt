BenchmarkBGERerankPair — microbench result
===========================================

Command:
  go test -bench BenchmarkBGERerankPair -benchtime=20x \
    -tags 'integration cgo darwin' ./pkg/memory/...

Environment:
  Host:       Apple M2 Max (darwin/arm64, 12 cores)
  Go:         (module oro, tags integration cgo darwin)
  Model:      ~/.oro/models/bge-reranker-base/model.onnx        (1.0 GB)
  Tokenizer:  ~/.oro/models/bge-reranker-base/tokenizer.json    (695 KB)
  ONNX Runtime dylib: ~/.oro/lib/onnxruntime.dylib (1.24.1, sourced locally)
  libtokenizers: linked statically from pkg/memory/lib/libtokenizers.a (no
    separate dylib required — see pkg/memory/cgo_ldflags.go)

Output:
  goos: darwin
  goarch: arm64
  pkg: oro/pkg/memory
  cpu: Apple M2 Max
  BenchmarkBGERerankPair-12          20      968542 ns/op
  PASS
  ok      oro/pkg/memory       1.729s

Per-pair latency:
  X = 968542 ns / 1e6 = 0.9685 ms

--fast sample size (per bead AC):
  size = max(5, min(10, floor(30000 / (20 * X))))
       = max(5, min(10, floor(30000 / 19.37)))
       = max(5, min(10, 1548))
       = 10

  --fast budget passes: floor(30000 / (20 * X)) = 1548 >> 5, well above the
  abort threshold. Hardware is fast enough for CI smoke; the 30 s budget
  could in principle run many more iterations, but the design caps the
  ceiling at 10 to keep --fast signal interpretable.

  ABORT branch (per AC) was NOT triggered: per-pair latency 0.97 ms is far
  below the 300 ms cutoff that would require faster hardware or a larger
  --fast budget.

Downstream: finalize `eval --fast --sample-size 10` in compare/cmd wiring.

---

NOTE on runtime prerequisite:
  The project's canonical ORT acquisition path is `make download-ort`, which
  is a TODO stub at Makefile:193-197 (no dylib acquisition logic yet). To
  run this bench locally, ~/.oro/lib/onnxruntime.dylib was populated
  manually from a compatible ORT 1.24.1 build. Once `make download-ort` is
  implemented, this bench should re-run and this note can be deleted.
  libtokenizers.dylib is NOT required at runtime — pkg/memory/cgo_ldflags.go
  links the libtokenizers.a static archive at build time.
